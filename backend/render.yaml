services:
  - type: web
    name: phoenix-datafeed
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: TWELVE_DATA_API_KEY
        sync: false
      - key: PORT
        value: 10000
