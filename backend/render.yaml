services:
  - type: web
    name: phoenix-datafeed
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: TWELVE_DATA_API_KEY
        sync: false
      - key: PORT
        value: 10000
